setup: ./setup.bash
run: ./run.bash {algorithm} {effort} {file}
factors:
  file: [sample.txt]
  algorithm: [gzip, zstd]
  effort: [default]

presets:
  smoke: {}
  full:
    factors:
      file: [sample.pb, sample.png, sample.txt]
      effort: [min, default, max]
    replicates: 6
